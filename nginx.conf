# nginx.conf — Customize domain and ports via variables
server {
  listen 80;
  server_name ${DOMAIN:-localhost};
  location / {
    proxy_pass http://app:3000;
    proxy_set_header Host $host;
  }
  # SSL placeholder — add certs via ${SSL_CERT}
}
